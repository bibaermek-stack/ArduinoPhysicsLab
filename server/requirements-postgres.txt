# Arduino Physics Lab — optional Production Postgres driver (Phase 9,
# Production Deployment Part J).
#
# NOT required for local development/tests — SQLite is the default
# (§ db/session.py::get_database_url(), falls back to a local .db file
# when DATABASE_URL is unset). Install this file ONLY on a deployment
# that sets DATABASE_URL to a real postgresql:// connection string:
#
#   pip install -r requirements.txt -r requirements-postgres.txt
#
# Kept as a separate file (rather than in the base requirements.txt) so
# the default SQLite-only install stays small and does not require a
# C-extension build toolchain on machines that never use Postgres.

psycopg2-binary
